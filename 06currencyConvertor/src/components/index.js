import InputBox from "./InputBox";

export { InputBox };

//this is the way to export multiple components from a single file, we can import them in other files like this: import { InputBox } from "./components";
//and this is the way which is used in bigger projects where we have multiple components in a single file, we can export them like this: export { InputBox, AnotherComponent, AnotherComponent2 };